# virtualenv

```bash
pip3 install virtualenv		#ubuntu
sudo pip3 install -i https://pypi.tuna.tsinghua.edu.cn/simple virtualenv virtualenvwrapper	#树莓派

virtualenv --version		#查看版本

virtualenv ENV				#创建环境名ENV
source ENV/bin/activate		#激活环境
deactivate					#取消激活
```

